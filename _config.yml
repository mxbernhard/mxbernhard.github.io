title: max bernhard # name of the site
author: Max Bernhard # name of site's author
email: max.bernhard@protonmail.com # email of site's author
url: https://mxbernhard.github.io # root address of the site
baseurl: "" # subpath of the site, e.g. "/blog" (leave it blank "" if you're site shouldn't use a subpath)
description: > # description of the site (multiple lines allowed)
  I'm a freelance journalist and researcher focused on online investigations and open-source intelligence (OSINT) reporting.
  
  a
  Previously I was a staff reporter for The Wall Street Journal and Dow Jones Newswires. 

permalink: /:slug.html

favicon: "logo.png" # name+extension of favicon (which must be put on the root folder)
goat_counter: "mxbernhard" # put your GoatCounter name if you want to use GoatCounter analytics

remote_theme: riggraz/no-style-please # if you are using GitHub Pages, change it to remote_theme: riggraz/no-style-please

theme_config:
  appearance: "light" # can be "light", "dark" or "auto"
  back_home_text: ".." # customize text for homepage link in post layout
  date_format: "%Y-%m-%d" # customize how date is formatted
  show_description: "true" # show blog description in home page

sass:
  style: :compressed

plugins:
  - jekyll-feed
  - jekyll-seo-tag
